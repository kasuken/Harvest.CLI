{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Harvest.CLI",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/bin/Debug/net10.0/Harvest.CLI.dll",
            "args": ["projects"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "stopAtEntry": false
        }
    ]
}
